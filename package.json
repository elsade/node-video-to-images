{
  "name": "split-video-to-frames",
  "version": "1.0.0",
  "description": "Simple CLI to split a video to png frames using ffmpeg",
  "main": "src/index.js",
  "scripts": {
    "clean": "rm -f ./output/*",
    "start": "NODE_ENV=development src/index.js",
    "start:dev": "nodemon",
    "test": "NODE_ENV=test test -c jest.config.json",
    "lint": "standard src/**/*.js",
    "prettier": "prettier --no-config 'src/**/*.js'"
  },
  "author": "Akrura Gordillo",
  "license": "ISC",
  "engines": {
    "yarn": "YARN NO LONGER USED - use npm instead."
  },
  "devDependencies": {
    "nodemon": "^1.18.6",
    "prettier": "1.15.1",
    "standard": "^12.0.1"
  },
  "dependencies": {
    "bluebird": "^3.5.3",
    "ffmpeg-static": "^2.3.0",
    "fluent-ffmpeg": "^2.1.2",
    "monet": "^0.8.10"
  }
}
